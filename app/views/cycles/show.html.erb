<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @cycle.name %>
</p>

<p>
  <strong>Questions:</strong><br>
  <% @cycleQuestions.each do |thisQ| %>
  	<%= thisQ.label %><br>
  <% end %>
</p>

<%= link_to 'Edit', edit_cycle_path(@cycle) %> |
<%= link_to 'Back', cycles_path %>
