<table class="table table-condensed">
  <thead>
    <tr>
      <td>Request</td>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% display.each do |display| %>
      <tr>
        <td><%= display.title %></td>
        <td><%= link_to 'Show', display %></td>
        <td><%= link_to 'Edit', edit_request_path(display) %></td>
        <td><%= link_to 'Destroy', display, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
