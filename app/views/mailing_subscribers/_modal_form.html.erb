

<%= form_for(@mailing_subscriber) do |f| %>
  <% if @mailing_subscriber.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mailing_subscriber.errors.count, "error") %> prohibited this mailing_subscriber from being saved:</h2>

      <ul>
      <% @mailing_subscriber.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<div class="form-group has-feedback">
   <label for="signupInputFirstName" class="text-muted">First Name</label>
   <%= f.text_field :first_name, class: "form-control", :placeholder => "Enter First Name", :required => "required" %>
   <span class="icon-people form-control-feedback text-muted"></span>
</div>

<div class="form-group has-feedback">
   <label for="signupInputLastName" class="text-muted">Last Name</label>
   <%= f.text_field :last_name, class: "form-control", :placeholder => "Enter Last Name", :required => "required" %>
   <span class="icon-people form-control-feedback text-muted"></span>
</div>

<div class="form-group has-feedback">
   <label for="signupInputEmail" class="text-muted">Email<span class="hidden-xs"> Address</span></label>
   <%= f.text_field :email, class: "form-control", :placeholder => "Email Address", :required => "required" %>
   <span class="icon-envelope form-control-feedback text-muted"></span>
</div>


  <div class="clearfix" style="display:none;">
    <div class="checkbox c-checkbox pull-left mt0">
       <label>
         <%= f.check_box :discount %>
          <span class="fa fa-check"></span>Gets Discount
       </label>
    </div>
  </div>


            <!-- From Modal -->
            </div>
         </div>
         <div class="modal-footer">
            <button type="button" data-dismiss="modal" class="btn btn-default">Close</button>
            <%= f.submit 'Sign Up!', :class => 'btn btn-success' %>
         </div>
      </div>
   </div>
</div>
<!-- End Modal Divs -->

<% end %>

<script>
  $('#mailing_subscriber_discount').prop('checked', true)
</script>