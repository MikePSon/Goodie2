<div class="form-group">
	<div class="row">
		<% if !current_user.applicant? %>
			<label class="col-sm-2 control-label">Type</label>
			<div class="col-sm-10">
				<div class="checkbox c-checkbox needsclick">
					<label class="needsclick">
						<%= f.check_box :program_admin, :class => "needsclick" %>
						<span class="fa fa-check"></span>Program Admin, the head honcho
					</label>
				</div>

				<!--<div class="checkbox c-checkbox">
					<label>
						<%= f.check_box :program_manager %>
						<span class="fa fa-check"></span>Program Manager, still doing good
					</label>
				</div>-->
			</div>
		<% end %>
	</div>
</div>
<script>
	$('#user_program_admin').prop('checked', true)
</script>